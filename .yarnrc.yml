nodeLinker: node-modules
enableScripts: true

# Optional but recommended for VS Code extensions with native deps:
supportedArchitectures:
  os:
    - "win32"
    - "linux"
    - "darwin"
  cpu:
    - "x64"
    - "arm64"